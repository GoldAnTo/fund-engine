# Fund Engine 设计文档

> 基于「文档1.md」的投资认知驱动型投研信息穿透系统

---

## 1. 背景与目标

### 1.1 问题诊断（来自文档1.md）

当前基金标签化、分类排名的工作陷入了「数据迷宫」，找不到出口：

- **基金只是壳子**：基金本质是一堆股票的组合，只研究基金 = 盲人摸象。买入时基金可能已经变了。见 [文档1.md#L44-L47](文档1.md#L44-L47)
- **后视镜数据不能线性外推**：过去涨得多 ≠ 未来涨得多，静态估值指标预测未来会出问题。见 [文档1.md#L54-L58](文档1.md#L54-L58)
- **出口 = 把「我相信什么」转换成匹配的投资方案**：从认知出发 → 作为假设放到 AI 验证 → 信息有左有右、无法分辨就靠「相信」→ 给出匹配认知的基金/股票清单。见 [文档1.md#L108-L118](文档1.md#L108-L118)

### 1.2 系统目标

构建一个**主题驱动的投研信息穿透系统**，把「认知 → 验证 → 匹配方案」这条路径产品化：

```
行业主题入口（如 AI 算力链）
    ↓
信息证据聚合（公告 / 研报 / 财报）
    ↓
关联穿透（产业链 / 基金持仓 → 股票）
    ↓
落到基金 / 股票 + 估值
    ↓
wiki 式可佐证、可解释的证据视图
```

系统要回答用户三个问题：
1. **我相信的这件事，证据在哪？**（公告/研报/财报，正反都有）
2. **它能穿透到哪些标的？**（产业链关联 → 股票 → 基金持仓）
3. **这些标的现在多贵？**（估值——文档反复强调的必答项）

### 1.3 核心设计原则（贯穿全系统）

| 原则 | 来源 | 体现 |
|------|------|------|
| 基金是壳子，要穿透 | 文档 L44-L47 | 基金 → 持仓股票 → 主题 |
| 不替用户下结论 | 文档 L118 | 正反证据都摆，「相信」留给用户 |
| 每个结论可溯源 | 文档 L116 | 每条证据带原文片段 + 溯源链接 |
| 估值是必答项 | 文档 L116 | 股票/基金节点带估值指标 |
| 主题可插拔 | 用户决策 | 主题作为一层节点，可动态新增 |

---

## 2. 系统架构

借鉴 FinGPT 的分层架构 + GraphRAG 的图谱+检索融合，分为五层：

```
┌─────────────────────────────────────────────────────────────┐
│  ⑤ 应用层：证据视图 API + wiki 式前端                         │
│     主题页 / 证据流 / 穿透链路 / 估值面板                      │
├─────────────────────────────────────────────────────────────┤
│  ④ 关联穿透与证据层                                           │
│     图查询引擎(多跳穿透) · 证据矛盾检测 · 置信度标记            │
├─────────────────────────────────────────────────────────────┤
│  ③ 知识图谱层（Neo4j）                                        │
│     实体 + 关系 + 证据节点 + 溯源边                            │
├─────────────────────────────────────────────────────────────┤
│  ② 信息抽取与图谱构建层                                       │
│     LLM 抽取(观点/实体/数值/倾向/原文片段) · 实体对齐 · 入图    │
├─────────────────────────────────────────────────────────────┤
│  ① 数据源接入层（已有接口）                                    │
│     公告 · 研报 · 财报 · 行情 · 基金持仓 · 估值数据            │
└─────────────────────────────────────────────────────────────┘
```

### 各层职责

- **① 数据源接入层**：对接团队已有的数据接口，统一抽象为 `DataSource` 适配器，屏蔽异构数据源差异。主题可插拔的关键：数据源按主题订阅。
- **② 信息抽取与图谱构建层**：LLM 从非结构化文档抽取结构化证据，写入图谱。借鉴 TrustRAG 的后生成引用、Verbatim RAG 的逐字片段防幻觉。
- **③ 知识图谱层**：Neo4j 存储实体、关系、证据、溯源。借鉴 fund_knowledge_graph 的数据模型。
- **④ 关联穿透与证据层**：Cypher 多跳查询实现穿透；证据矛盾检测（借鉴 GraphRAG Agent）识别正反冲突并标记置信度。
- **⑤ 应用层**：FastAPI 提供 REST 接口，支撑证据视图前端。

---

## 3. 数据模型（图谱 Schema）

借鉴 [fund_knowledge_graph](https://github.com/green23/fund_knowledge_graph) 并扩展。核心思路：**证据（Claim）作为一等公民节点**，把信息、实体、溯源串起来。

### 3.1 节点（Node）

| 标签 | 说明 | 关键属性 |
|------|------|----------|
| `Theme` | 行业主题（可插拔） | id, name, description, hypothesis(认知假设), created_at |
| `Industry` | 行业 | code, name, level |
| `Company` | 公司（上市/基金管理/托管） | code, name, type |
| `Stock` | 股票 | code, name, market, pe, pb, roe, market_cap, valuation_updated_at |
| `Fund` | 基金 | code, name, scale, establish_date, fund_type |
| `FundCompany` | 基金管理公司 | code, name |
| `Person` | 人物（基金经理/高管） | id, name, role |
| `Document` | 信息源文档 | id, type(公告/研报/财报), title, source, publish_date, url, raw_text |
| `Claim` | 证据/观点（核心） | id, content(观点), snippet(原文逐字片段), sentiment(正/反/中性), confidence, metrics{pe,pb,...}, extracted_at |
| `Metric` | 指标（估值/财务） | id, name, value, period |

### 3.2 关系（Relationship）

```
穿透链路（核心）：
  (Theme)-[INCLUDES {weight}]->(Stock)          主题包含股票
  (Stock)-[BELONGS_TO]->(Company)                股票属于公司
  (Stock)-[IN_INDUSTRY]->(Industry)              股票属某行业
  (Fund)-[HOLDS {weight, as_of_date}]->(Stock)   基金持仓股票（含权重）
  (Fund)-[MANAGED_BY]->(FundCompany)            基金管理公司
  (Fund)-[MANAGED_BY {since}]->(Person)         基金经理

产业链关联：
  (Stock)-[SUPPLIES {product}]->(Stock)          上游供应下游
  (Stock)-[COMPETES_WITH]->(Stock)               竞争关系
  (Stock)-[CUSTOMER_OF]->(Stock)                 客户关系

证据与溯源（核心）：
  (Document)-[CLAIMS]->(Claim)                   文档提出观点
  (Claim)-[ABOUT]->(Stock|Theme|Industry)        观点关于谁
  (Claim)-[SUPPORTS]->(Claim)                    观点相互佐证
  (Claim)-[CONTRADICTS]->(Claim)                 观点相互矛盾
  (Claim)-[CITES {span}]->(Document)             溯源到原文段落
  (Claim)-[REFERS_METRIC]->(Metric)              观点引用的指标
```

### 3.3 图谱示例（AI 算力链主题）

```
[Theme: AI算力链]
   │ INCLUDES
   ├─[Stock: 寒武纪]─BELONGS_TO─[Company: 寒武纪]─IN_INDUSTRY─[Industry: 芯片]
   │     ↑ HOLDS(weight=8.2%)
   │  [Fund: 某AI主题基金]
   │
   ├─[Stock: 工业富联]─SUPPLIES─[Stock: 寒武纪]   (产业链：代工)
   │
   └─ 证据簇:
        [Document: 中信研报]─CLAIMS─[Claim: "AI算力需求高增长"(正,conf=0.9)]
                                       │ ABOUT → [Stock: 寒武纪]
                                       │ CITES(span="...三年CAGR 45%...") → Document
                                       │ SUPPORTS ← [Claim: 国信研报观点]
                                       │ CONTRADICTS → [Claim: "估值已透支"(反,conf=0.7)]
```

---

## 4. 证据抽取管道

借鉴 [TrustRAG](https://arxiv.org/html/2502.13719v1/)（后生成引用）+ [Verbatim RAG](https://pypi.org/project/verbatim-rag/)（逐字片段防幻觉）。

### 4.1 流程

```
原始文档(公告/研报/财报)
   ↓ 解析（PDF/HTML → 结构化文本，保留段落定位）
结构化文本
   ↓ LLM 抽取（Prompt 见 4.2）
[Claim] × N：观点 + 实体 + 数值 + 倾向 + 原文片段 + 段落定位
   ↓ 实体对齐（"寒武纪"="688256.SH"，"米妮max"="MiniMax"）
   ↓ 矛盾检测（与已有 Claim 比较，标注 SUPPORTS/CONTRADICTS）
写入 Neo4j（Claim 节点 + 溯源边）
```

### 4.2 LLM 抽取 Prompt 结构（示意）

```
输入：文档段落 D
输出：JSON 数组，每个元素：
{
  "content": "AI算力链景气度持续向上",      // 观点
  "entities": ["寒武纪", "AI算力"],          // 涉及实体
  "metrics": {"cagr": "45%", "period": "3年"}, // 引用数值
  "sentiment": "positive",                  // 倾向：positive/negative/neutral
  "snippet": "我们预计未来三年算力需求CAGR达45%", // 原文逐字片段
  "span": {"para": 3, "start": 12, "end": 31} // 段落定位，用于溯源点击
}
```

### 4.3 矛盾检测（对应文档「信息有左有右」）

借鉴 [GraphRAG Agent](https://juejin.cn/post/7517501712572153882)：
- 新 Claim 入图时，检索同主题、同实体的已有 Claim
- LLM 判断语义关系：`SUPPORTS` / `CONTRADICTS` / 无关
- 矛盾的 Claim 对在证据视图中高亮，并标注双方置信度
- **不自动裁决**，正反都展示，让用户决定相信谁

---

## 5. 穿透链路

回应文档「基金是壳子」的核心痛点。

### 5.1 正向穿透（主题 → 基金）

```
输入：Theme = "AI算力链"
查询：Theme -INCLUDES-> Stock <-HOLDS- Fund
输出：持有该主题股票的基金清单，按主题股权重加总排序
```

Cypher 示意：
```cypher
MATCH (t:Theme {name: $theme})-[:INCLUDES]->(s:Stock)<-[h:HOLDS]-(f:Fund)
RETURN f.code, f.name, sum(h.weight) AS theme_exposure
ORDER BY theme_exposure DESC
```

### 5.2 反向穿透（基金 → 真正装了什么）

```
输入：Fund = "某基金"
查询：Fund -HOLDS-> Stock -IN_INDUSTRY-> Industry, Stock <-INCLUDES- Theme
输出：这只基金「真正装了什么」——持仓股票、行业分布、命中的主题
```

### 5.3 产业链穿透

```
输入：Stock = "寒武纪"
查询：Stock <-SUPPLIES- Stock（上游），Stock -SUPPLIES-> Stock（下游）
输出：产业链上下游，可扩展到 N 跳
```

---

## 6. 证据视图

用户最想先看到的部分。一个主题页的布局：

```
┌──────────────────────────────────────────────────────────────┐
│  主题：AI 算力链          认知假设：算力需求未来3年CAGR 45%      │
├──────────────────┬───────────────────────┬───────────────────┤
│  认知假设验证     │  证据流（可溯源）        │  穿透结果          │
│                  │                       │                   │
│ ☑ 景气度向上     │ [研报] 算力需求CAGR 45% │ 关联股票:          │
│   ├ 3条佐证      │  正·中信·conf 0.9      │  寒武纪 PE 300     │
│   └ 1条反证      │  [原文片段] [溯源↗]     │  工业富联 PE 25    │
│                  │                       │                   │
│ ☐ 估值未透支     │ [研报] 估值已透支       │ 持有基金(Top5):    │
│   ├ 2条佐证      │  反·国信·conf 0.7      │  某AI基金 8.2%     │
│   └ 3条反证 ⚠矛盾│  [原文片段] [溯源↗]     │  某成长混合 5.1%   │
│                  │  ⚠ 与上方观点冲突       │                   │
│                  │                       │ 产业链:            │
│                  │ [财报] 营收同比+120%    │  上游: 工业富联    │
│                  │  正·财报·conf 0.95     │  下游: 字节跳动    │
│                  │  [原文片段] [溯源↗]     │                   │
└──────────────────┴───────────────────────┴───────────────────┘
```

关键交互：
- 每条证据可点击 `[溯源↗]` 跳转原文对应段落
- 矛盾证据高亮 `⚠`，点开看双方论据
- 右侧股票/基金带估值（PE/PB/ROE），呼应「多贵」必答项

---

## 7. API 设计（FastAPI）

### 7.1 主题

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/themes` | 主题列表 |
| POST | `/api/themes` | 新增主题（可插拔） |
| GET | `/api/themes/{id}` | 主题详情 + 认知假设 |

### 7.2 证据

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/themes/{id}/evidence` | 主题下证据流，支持 `sentiment`、`entity` 过滤 |
| GET | `/api/claims/{id}` | 证据详情 + 溯源原文 |
| GET | `/api/claims/{id}/conflicts` | 该证据的矛盾证据 |

### 7.3 穿透

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/themes/{id}/stocks` | 主题 → 关联股票 + 估值 |
| GET | `/api/themes/{id}/funds` | 主题 → 命中基金（按主题暴露度排序） |
| GET | `/api/funds/{code}/holdings` | 基金 → 持仓股票 + 命中主题（反向穿透） |
| GET | `/api/stocks/{code}/supply-chain` | 股票 → 产业链上下游 |

### 7.4 证据抽取（内部）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/ingest/document` | 提交文档，触发抽取管道 |

### 响应示例（证据流）

```json
{
  "theme_id": "ai-compute",
  "claims": [
    {
      "id": "c001",
      "content": "算力需求未来三年CAGR达45%",
      "sentiment": "positive",
      "confidence": 0.9,
      "document": {"type": "研报", "source": "中信证券", "date": "2025-07-01", "url": "..."},
      "snippet": "我们预计未来三年算力需求CAGR达45%",
      "span": {"para": 3, "start": 12, "end": 31},
      "entities": ["寒武纪"],
      "conflicts": ["c007"]
    }
  ]
}
```

---

## 8. MVP 范围

**目标**：用样例数据跑通「主题 → 证据 → 穿透 → 视图」闭环，不接真实数据接口。

### MVP 包含
- 图谱 schema 落地（Neo4j）
- 证据抽取管道（LLM 抽取 + 入图）
- 正向穿透（主题 → 基金）
- 反向穿透（基金 → 持仓）
- 证据视图 API（主题证据流 + 溯源）
- 矛盾检测（SUPPORTS/CONTRADICTS）

### MVP 不包含
- 实时数据接入（后续对接已有接口）
- 产业链 N 跳穿透（先做 1 跳）
- 前端可视化（先出 API + 简单页面）
- 估值实时更新

### MVP 数据
- 准备 1 个主题（如 AI 算力链）的样例：3-5 只股票、2-3 只基金、5-10 份文档（公告/研报/财报各几份）

---

## 9. 技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| 后端 | Python 3.11 + FastAPI | LLM 生态最全，用户选定 |
| 图数据库 | Neo4j 5.x | 生态成熟，fund_knowledge_graph 同款，MVP 合适 |
| 图驱动 | neo4j-python-driver | 官方驱动 |
| LLM | 可配置（OpenAI/通义/DeepSeek） | 抽取与矛盾检测 |
| LLM 编排 | LangChain（轻量使用） | Prompt 管理 + 结构化输出 |
| 向量检索 | Neo4j 向量索引（内置） | 不引入额外组件，证据语义检索 |
| 文档解析 | 适配器模式，MVP 用纯文本 | 后续接 PDF/HTML |
| 前端 | 待定（证据视图） | MVP 先 API + 简单 HTML |

---

## 10. 借鉴的开源项目

| 需求 | 项目 | 借鉴点 |
|------|------|--------|
| 基金→股票穿透 | [fund_knowledge_graph](https://github.com/green23/fund_knowledge_graph) | 数据模型、Neo4j、Tushare |
| 证据带溯源 | [TrustRAG](https://arxiv.org/html/2502.13719v1/) | 后生成引用、句子级引用 |
| 防幻觉 | [Verbatim RAG](https://pypi.org/project/verbatim-rag/) | 逐字片段 + 直接引用 |
| 正反矛盾检测 | [GraphRAG Agent](https://juejin.cn/post/7517501712572153882) | 证据矛盾检测 + 置信度 |
| 主题可插拔 | [微软 GraphRAG](https://github.com/microsoft/graphrag) | 社区聚类 = 主题子图 |
| 投研分层架构 | [FinGPT](https://github.com/AI4Finance-Foundation/FinGPT) | 4 层架构 |
| 多步可解释 | [TradingAgents](https://wenku.csdn.net/doc/60w9a0z15k) | 决策溯源图谱 |

---

## 11. 待确认问题

1. **已有数据接口的形态**：是 REST API、数据库直连、还是文件？这决定数据源适配器的实现。
2. **LLM 选型**：用哪家？是否有预算/合规要求？影响抽取质量与成本。
3. **估值数据来源**：股票 PE/PB/ROE 是否在已有接口里？
4. **前端形态**：证据视图是独立 Web 应用，还是嵌入现有系统？
5. **文档解析**：公告/研报/财报是 PDF 还是 HTML？是否带结构？
