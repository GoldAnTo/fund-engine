# Vite server-only configuration. Copy to .env.local; never commit real tokens.
VITE_API_BASE=http://127.0.0.1:8000
# Must match a token in backend RESEARCH_TENANT_TOKENS. No VITE_ prefix.
RESEARCH_BEARER_TOKEN=
