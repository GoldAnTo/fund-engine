# 检测路线实施跟踪

用户授权：按照完整检测报告的修改路线继续修复。目标保留报告第5节四阶段及其验收条件，不以某批测试通过替代完整目标。

## 第一阶段：真实入口与访问边界
- [x] 默认真实API工作台；演示必须显式选择，失败不回退。
- [x] 研究列表/详情/事件创建，URL恢复，错误保留输入，过期响应保护。
- [x] 开发代理服务端认证，浏览器不持有宿主Bearer配置。
- [ ] 路由租户矩阵：Case、thesis、document、review、job、共享目录逐类归属。已完成jobs、review、Case文档引用、文档库读取、文档抽取/补充写入与engine/causal、task/activity；共享目录策略仍待实施。
- [x] 已修复job/review/Case文档路径的owner/foreign/anonymous验证；任务取消/重试不得跨租户。其余路径跟随矩阵逐片补齐。
- [x] 重复提交幂等和真实HTTP浏览器验证。

## 第二阶段：研究闭环
- [ ] 补材料/抽取/待审核与人工决策。已接普通补材料、准备读取/失败重试及可见已审计划授权；陈述审核和协议分组编辑确认已接；证据审核已接接受/拒绝/修改并通过真实浏览器；真实完整协议浏览器验收仍待完成。
- [ ] 真实任务进度、取消/重试和刷新恢复。已接真实run列表/历史分页、详情事件分页、取消与刷新恢复并通过真实浏览器；运行重试语义仍待完成。
- [x] 公司研究冻结revision、历史回放、真实导出：恢复完整入口，隔离真实浏览器与下载哈希验收通过。
- [ ] 恢复Company Research与事件研究live verifier及CI，不以占位脚本通过旧测试。Company Research完整verifier及71项支持测试已恢复并通过；公司CI已接入并本地顺序验证；GitHub运行未执行，事件完整verifier待补。

## 第三阶段：LLM与worker
- [x] 统一严格schema及无效输出状态。
- [x] 永久/瞬态错误分类、退避；评估与合规rewrite共享操作预算。同步I/O不能硬取消，迟到结果拒绝，未宣称硬deadline。
- [ ] usage/预算审计。run_ai_engine已知provider失败的安全CLI边界已完成，输入/响应解析字节边界已补，已持久化extract/propose/assess请求用量（0071，未知不按零计）；其他调用路径、run/Case聚合与任务成本门禁仍待实施。
- [x] 真实PostgreSQL隔离并发验证：全部35项pg_only通过，修复同key重放旧状态与测试隔离，接入CI；托管CI尚未执行。

## 第四阶段：性能、可访问性与运维
- [ ] 代表性数据分页/查询计划与队列延迟评估。job事件已SQL分页并以1003条历史数据验证加载上界，其余查询/规模仍待测。
- [ ] 上传解析资源边界和清理。
- [ ] readiness、结构化指标、服务告警。/ready已验证数据库与当前迁移版本，并在临时PostgreSQL验收；结构化指标和告警仍待完成。
- [ ] 200%缩放、键盘/阅读器、视觉收尾。

验证：每项以失败回归、最小实现、相关测试及必要真实浏览器/隔离服务证据更新。本目标保持active，只有完整需求被当前状态证实才完成。

## 本批验证与下一步

实现与证据汇总见 [修复进展](../../evaluation/2026-09-06-roadmap-progress.md)。下一批先完成剩余权限路由与审核UI闭环，再恢复旧完整live verifier；不可用本批入口E2E替代Company Research端到端验收。
