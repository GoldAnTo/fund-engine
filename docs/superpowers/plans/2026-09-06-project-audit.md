# 全面检测与第一批修复计划

目标：基于当前 checkout 建立测试基线，覆盖 UI、操作语义、后端、LLM、发布链路，并实施有复现依据的修复。

架构选择：保留 FundClaw 当前界面和后端账本，不恢复旧前端、不迁移真实数据。比单纯样式调整更优先修正数据隔离、错误成功状态和运行可靠性；全面接回 API 是独立后续工程，需要完整页面契约与验收。

用户已经明确要求开始实施，以下可逆修复在当前授权范围内。采用 systematic-debugging 和 test-driven-development：先失败回归，再最小修复，再相关回归；最终运行整合验证。

- [x] 检查 Git 状态、产品文档、前后端入口、测试与 CI 命令。
- [x] 前端基线：4 tests passed，build passed。
- [x] 后端全量 pytest，记录全部失败与跳过条件。
- [x] 浏览器复现线程串数据、归档状态错误、390px 侧栏遮挡。
- [x] 前端：演示边界、线程消息与草稿隔离、真实筛选和计数、无数据空态、禁用未实现操作、响应布局、键盘与减少动态效果。
- [x] LLM：拒绝被截断、拒答及非正常结束的响应，避免 JSON 修复掩盖不完整结果。
- [x] Worker：wait_for_sources 校验 claim token，禁止过期 worker 清除新任务所有权。
- [x] API：对已确认的旧工作台读取与 thesis 写入绕过增加认证与 case ownership 校验，覆盖 owner/foreign/anonymous。
- [x] 工程：恢复当前前端 E2E 命令与 CI 构建验证；记录被重建移除的真实联调能力，避免伪造验收通过。
- [x] 汇总完整中文报告：优先级、证据、现有保障、修改状态、后续实施顺序、测试边界。

文件责任：前端修复仅 frontend/src 与 index；LLM client 与对应测试；worker repository/调用与对应测试；主代理负责 API 授权、工程配置、整合报告及最终复核。

完成说明：检测与第一批修复已实施；真实前端接回API、其余路由租户边界、LLM预算等后续建议不在本批完成范围。原全量9项失败中6项已修，3项旧前端验收缺失明确保留，详见总报告。
