# 旧前端完整清理记录（2026-09-10）

> 后续[公司研究主线整合](2026-09-10-company-mainline-integration.md)保持了本次清理结果，并将运行校验器的固定迁移版本改为读取仓库唯一 head。下文的版本和测试数字记录清理当时的状态。

本轮按用户要求，在讨论新原型之前清理旧界面。新界面尚未设计或实现。

## 清理范围

- 覆盖 46 个本地分支的最新版本、35 个工作目录，以及现有 14 个远端分支。
- 移除完整 `frontend/`、`prototype/`、旧 brainstorm、文档中的原型 HTML、旧页面辅助代码、浏览器验收脚本、依赖和构建产物。
- 移除旧分支中的前端 CI、Compose 服务及启动、恢复、构建入口；历史分支保留其后端差异，不再承担旧界面验收。
- 项目内的外部参考前端随完整参考项目一起迁入项目外备份。
- 停止本项目旧前端进程，移除 3 个旧前端容器和 9 个已核实包含旧界面的镜像；删除前已导出并校验镜像。

Git 历史保留，清理以追加提交完成。历史提交及项目外备份中的旧代码仅用于恢复，不是现行界面。

## 保留内容

后端研究逻辑、数据库迁移、研究资料和数据卷保留。研究报告的 HTML、PDF 和原文图片属于研究产物，继续保留。

`main` 和 `codex/company-research-prototype-delivery` 的 API 客户端与契约迁至 `clients/research/`，没有页面、路由、开发服务器或界面构建命令。实际 HTTP 客户端和契约内容保留；目录加深导致的一处测试数据引用同步调整。

这两个主要版本使用 `deploy/api-proxy/` 提供 `/health` 和 `/api/`，其他路径返回 404。历史分支仅保留各自后端运行配置。

## 验证范围

- 主目录客户端：类型检查通过，109 项测试通过。
- 研究交付目录客户端：类型检查通过，128 项测试通过。
- 两个主要目录各 232 项相关后端、运行脚本与契约测试通过。
- 实际临时 Nginx 容器的上传边界及旧路由 404 检查通过。
- 四个现有后端健康端点均返回 200；数据卷清单保持 26 个。
- Git 分支、工作目录、启动入口、Docker 容器与镜像另作最终清单核对，记录保存在项目外备份目录。

以上是界面退役及后端保留检查，不代表新界面、浏览器研究业务流程或线上 LLM 研究质量已经验收。CI 保留仓库要求的 `Playwright e2e` 状态名，实际只执行旧界面清理检查，不运行 Playwright。

既有 `verify-one-click-runtime.sh` 的迁移版本断言仍固定为 `0070`，不属于此次界面清理的修改范围；本轮没有据此声明当前完整部署流程已验收。其他机器如仍运行旧前端容器，应先退役其精确容器，避免占用 API 代理端口。

## 备份

完整仓库 bundle、未提交源文件、旧原型、镜像、参考项目及逐文件校验记录保存于：

`$HOME/.codex/backups/fund-engine/20260910-complete-frontend-cleanup/`

该备份位于项目目录之外，没有将原始配置、研究材料或额外的未审计历史分支发布到公共远端。远端仅同步原来已有的 14 个分支。
